---
phase: 06-productiestructuur-config-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phantom_tool.py
autonomous: true
requirements: [PST-01, PST-02]

must_haves:
  truths:
    - "productiestructuur.exe leest SQL server, credentials en databases uit config.json naast de exe"
    - "Bij opstart checkt productiestructuur.exe version.json op de netwerk share met 2s timeout"
    - "Als er een nieuwere versie is, toont de tool een CTk update dialog met Updaten/Later knoppen"
    - "Bij klikken op Updaten wordt de exe gekopieerd en herstart de tool"
    - "Als de share niet bereikbaar is, start de tool gewoon op zonder foutmelding"
    - "Als config.json ontbreekt, opent de settings dialog automatisch"
    - "Omgevingskeuze (Speel/Live) leest database namen uit config.json"
    - "Titelbalk toont Productiestructuur v1.0.0"
  artifacts:
    - path: "scripts/phantom_tool.py"
      provides: "Productiestructuur tool met config.json integratie en self-update"
      contains: "__version__ = '1.0.0'"
  key_links:
    - from: "scripts/phantom_tool.py"
      to: "scripts/app_config.py"
      via: "import check_for_update, do_self_update, show_update_dialog, config_exists, show_settings_dialog, get_connection, load_config"
      pattern: "from app_config import"
    - from: "scripts/phantom_tool.py::_check_update_before_gui()"
      to: "app_config::check_for_update()"
      via: "function call with tool_name='productiestructuur'"
      pattern: "check_for_update\\('productiestructuur'"
    - from: "scripts/phantom_tool.py::_show_update_after_gui()"
      to: "app_config::show_update_dialog() + do_self_update()"
      via: "app.after(100, ...) callback"
      pattern: "app\\.after\\(100"
---

<objective>
Refactor phantom_tool.py to use config.json for all settings and add the proven self-update pattern from bom-import-tool.

Purpose: productiestructuur.exe wordt een zelfstandige tool die config.json leest, bij opstart op updates checkt, en de settings dialog toont als config.json ontbreekt -- identiek gedrag als bom-import-tool.

Output: scripts/phantom_tool.py met v1.0.0, config.json integratie, en twee-fase self-update
</objective>

<execution_context>
@C:/Users/rik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-self-update/05-02-SUMMARY.md
@scripts/phantom_tool.py
@scripts/main.py
@scripts/app_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config integratie en self-update in phantom_tool.py</name>
  <files>scripts/phantom_tool.py</files>
  <action>
Refactor phantom_tool.py met de volgende wijzigingen:

1. **Versie bump**: `__version__ = '0.1.0'` -> `__version__ = '1.0.0'`

2. **Import uitbreiden**: Voeg toe aan bestaande import van app_config:
   `from app_config import get_connection, load_config, config_exists, show_settings_dialog, check_for_update, do_self_update, show_update_dialog`

3. **Omgevingskeuze uit config**: In `PhantomApp._build_ui()`, de radio buttons voor Speel/Live moeten hun labels lezen uit config.json. In de toolbar sectie waar de radio buttons staan, laad `config = load_config()` en gebruik `config['databases']` keys voor de waarden. De standaard `value="speel"` blijft, maar voeg een tooltip of label toe dat de database naam toont.

4. **Vervang _check_update()**: Verwijder de bestaande `_check_update()` functie (regels 805-829) en vervang door het BEWEZEN twee-fase patroon uit main.py:

   ```python
   def _check_update_before_gui() -> dict | None:
       """Check voor updates op de network share (voor de GUI start).
       Returns update info dict als er een update is, None anders.
       """
       try:
           from app_config import check_for_update
           update = check_for_update('productiestructuur', __version__)
           if update:
               return update
           return None
       except Exception:
           return None

   def _show_update_after_gui(app, remote_version, resolved_share):
       """Toon update dialog met de App als parent (na mainloop start)."""
       try:
           from app_config import show_update_dialog, do_self_update
           ok = show_update_dialog(__version__, remote_version, parent=app)
           if ok and do_self_update('productiestructuur', 'productiestructuur.exe',
                                    share_override=resolved_share):
               app.destroy()
               sys.exit(0)
       except Exception:
           pass
   ```

5. **Vervang main()**: De huidige `main()` roept `_check_update()` aan en dan `PhantomApp()`. Vervang door:

   ```python
   def main():
       update_info = _check_update_before_gui()
       app = PhantomApp()
       if update_info:
           try:
               app.after(100, lambda: _show_update_after_gui(
                   app, update_info['remote_version'], update_info['update_share']))
           except Exception:
               pass
       app.mainloop()
   ```

6. **Titelbalk**: De titel is al `f"{APP_TITLE} v{__version__}"` -- na de versie bump wordt dit automatisch "Productiestructuur v1.0.0". Geen wijziging nodig.

**Cruciale details (geleerd uit Phase 5):**
- tool_name = `'productiestructuur'` (moet matchen met key in version.json)
- exe_name = `'productiestructuur.exe'`
- share_override parameter MOET meegegeven worden aan do_self_update (anders dubbele UNC resolve)
- De twee-fase splitsing is VERPLICHT: CTkToplevel vereist actieve App als parent
- app_config.py hoeft NIET aangepast te worden -- alle functies zijn al tool-agnostisch
  </action>
  <verify>
    <automated>cd "C:/Users/rik/ritco.be/Ritco BV/Spinnekop/scripts" && python -c "import phantom_tool; assert phantom_tool.__version__ == '1.0.0'; print('Version OK')" && python -c "from phantom_tool import _check_update_before_gui, _show_update_after_gui; print('Two-phase update functions exist')" && python -c "import phantom_tool; import inspect; src = inspect.getsource(phantom_tool.main); assert '_check_update_before_gui' in src; assert 'update_info' in src; print('main() uses two-phase pattern')"</automated>
    <manual>Controleer dat phantom_tool.py geen hardcoded database namen meer bevat en dat _check_update (de oude functie met tkinter messagebox) verwijderd is</manual>
  </verify>
  <done>phantom_tool.py v1.0.0 met config.json integratie, twee-fase self-update (identiek patroon als main.py), en settings dialog bij ontbrekende config</done>
</task>

</tasks>

<verification>
- `python -c "import phantom_tool; print(phantom_tool.__version__)"` geeft `1.0.0`
- `_check_update_before_gui()` en `_show_update_after_gui()` bestaan als top-level functies
- `main()` volgt het twee-fase patroon: check -> App() -> after(100, show dialog)
- Geen tkinter.Tk() root meer in de update check (de oude pattern)
- config_exists() check in PhantomApp.__init__() is behouden
- show_settings_dialog() callback in _open_settings() is behouden
</verification>

<success_criteria>
- phantom_tool.py compileert zonder fouten (`python -c "import phantom_tool"`)
- Versie is 1.0.0
- Self-update gebruikt het bewezen twee-fase patroon uit main.py
- Alle bestaande functionaliteit (tree view, phantom toggles, settings) is intact
</success_criteria>

<output>
After completion, create `.planning/phases/06-productiestructuur-config-update/06-01-SUMMARY.md`
</output>
