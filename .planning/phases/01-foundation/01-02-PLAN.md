---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/setup-server-structure.ps1
autonomous: false

must_haves:
  truths:
    - "C:\\import\\bom-import-tool.exe bestaat en is de actieve stable versie"
    - "C:\\import\\archive\\ bestaat voor toekomstige rollbacks"
    - "C:\\import-test\\ bestaat met speel\\ en live\\ submappen voor test-output"
    - "Oude map C:\\import\\bom-import-tool\\ is verwijderd"
    - "C:\\Users\\Public\\Desktop\\BOM Import Tool.lnk bestaat en opent C:\\import\\bom-import-tool.exe"
  artifacts:
    - path: "scripts/setup-server-structure.ps1"
      provides: "Server setup script — migratie, mappenstructuur, snelkoppeling"
      contains: "Public\\Desktop"
  key_links:
    - from: "C:\\Users\\Public\\Desktop\\BOM Import Tool.lnk"
      to: "C:\\import\\bom-import-tool.exe"
      via: "Windows Shortcut TargetPath"
      pattern: "TargetPath.*C:\\\\import\\\\bom-import-tool.exe"
---

<objective>
Schrijf een PowerShell setup-script dat de server-mappenstructuur aanmaakt, de huidige exe migreert naar de nieuwe stable locatie, en een Public Desktop snelkoppeling aanmaakt voor Evy (en Rik).

Purpose: SRV-01, SRV-02, SRV-03. Evy heeft een snelkoppeling die altijd naar stable wijst; stable en dev zijn gescheiden; rollback-archief is klaar voor gebruik.
Output: scripts/setup-server-structure.ps1 op Z: gezet, uitgevoerd via RDP op VMSERVERRUM.
</objective>

<execution_context>
@C:/Users/rik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@scripts/connect-ridderiq.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schrijf setup-server-structure.ps1</name>
  <files>scripts/setup-server-structure.ps1</files>
  <action>
Maak een nieuw PowerShell script `scripts/setup-server-structure.ps1` dat het volgende doet op VMSERVERRUM:

**Doel:** Eenmalig uitvoeren via RDP als ridderadmin. Idempotent — veilig opnieuw uitvoeren.

```powershell
#Requires -RunAsAdministrator
<#
.SYNOPSIS
    Eenmalige server setup voor BOM Import Tool release structuur.
    Uitvoeren via RDP op VMSERVERRUM als ridderadmin.

.DESCRIPTION
    1. Maakt C:\import\archive\ aan
    2. Maakt C:\import-test\ aan met output submappen
    3. Migreert exe van oude naar nieuwe locatie
    4. Verwijdert lege oude map
    5. Maakt Public Desktop snelkoppeling aan

    Idempotent: veilig om meerdere keren uit te voeren.
#>

$ErrorActionPreference = "Stop"

Write-Host "=== BOM Import Tool — Server Setup ===" -ForegroundColor Cyan
Write-Host ""

# --- 1. Archief map aanmaken ---
Write-Host "[1/5] Archive map aanmaken..." -ForegroundColor Yellow
$archivePath = "C:\import\archive"
if (-not (Test-Path $archivePath)) {
    New-Item -ItemType Directory -Path $archivePath | Out-Null
    Write-Host "      Aangemaakt: $archivePath" -ForegroundColor Green
} else {
    Write-Host "      Bestaat al: $archivePath" -ForegroundColor Gray
}

# --- 2. Import-test root aanmaken met output submappen ---
Write-Host "[2/5] Import-test structuur aanmaken..." -ForegroundColor Yellow
$testPaths = @(
    "C:\import-test",
    "C:\import-test\speel",
    "C:\import-test\live"
)
foreach ($path in $testPaths) {
    if (-not (Test-Path $path)) {
        New-Item -ItemType Directory -Path $path | Out-Null
        Write-Host "      Aangemaakt: $path" -ForegroundColor Green
    } else {
        Write-Host "      Bestaat al: $path" -ForegroundColor Gray
    }
}

# --- 3. Exe migreren ---
Write-Host "[3/5] Exe migreren naar nieuwe locatie..." -ForegroundColor Yellow
$oldExe = "C:\import\bom-import-tool\bom-import-tool.exe"
$newExe = "C:\import\bom-import-tool.exe"

if (Test-Path $oldExe) {
    if (Test-Path $newExe) {
        Write-Host "      Nieuwe locatie bestaat al — migratie overgeslagen" -ForegroundColor Gray
        Write-Host "      (verwijder $newExe handmatig als je opnieuw wilt migreren)" -ForegroundColor Gray
    } else {
        Copy-Item -Path $oldExe -Destination $newExe
        Write-Host "      Gekopieerd: $oldExe -> $newExe" -ForegroundColor Green
    }
} elseif (Test-Path $newExe) {
    Write-Host "      Exe al op nieuwe locatie, niets te migreren" -ForegroundColor Gray
} else {
    Write-Host "      WAARSCHUWING: Geen exe gevonden op oude of nieuwe locatie." -ForegroundColor Red
    Write-Host "      Kopieer handmatig de exe naar: $newExe" -ForegroundColor Red
}

# --- 4. Lege oude map verwijderen ---
Write-Host "[4/5] Oude map opruimen..." -ForegroundColor Yellow
$oldDir = "C:\import\bom-import-tool"
if (Test-Path $oldDir) {
    $contents = Get-ChildItem -Path $oldDir
    if ($contents.Count -eq 0) {
        Remove-Item -Path $oldDir
        Write-Host "      Verwijderd (was leeg): $oldDir" -ForegroundColor Green
    } else {
        Write-Host "      Map bevat nog bestanden — niet verwijderd:" -ForegroundColor Yellow
        $contents | ForEach-Object { Write-Host "        $_" -ForegroundColor Yellow }
        Write-Host "      Controleer en verwijder handmatig als alles is gemigreerd." -ForegroundColor Yellow
    }
} else {
    Write-Host "      Bestaat niet meer: $oldDir" -ForegroundColor Gray
}

# --- 5. Public Desktop snelkoppeling aanmaken ---
Write-Host "[5/5] Public Desktop snelkoppeling aanmaken..." -ForegroundColor Yellow
$shortcutPath = "C:\Users\Public\Desktop\BOM Import Tool.lnk"
$targetExe = "C:\import\bom-import-tool.exe"

$WShell = New-Object -ComObject WScript.Shell
$shortcut = $WShell.CreateShortcut($shortcutPath)
$shortcut.TargetPath = $targetExe
$shortcut.WorkingDirectory = "C:\import"
$shortcut.Description = "BOM Import Tool — Spinnekop"
$shortcut.IconLocation = "$targetExe,0"
$shortcut.WindowStyle = 1
$shortcut.Save()

if (Test-Path $shortcutPath) {
    Write-Host "      Snelkoppeling aangemaakt: $shortcutPath" -ForegroundColor Green
    Write-Host "      -> Wijst naar: $targetExe" -ForegroundColor Green
} else {
    Write-Host "      FOUT: Snelkoppeling aanmaken mislukt!" -ForegroundColor Red
    exit 1
}

# --- Verificatie ---
Write-Host ""
Write-Host "=== Verificatie ===" -ForegroundColor Cyan
$checks = @(
    @{ Path = "C:\import\bom-import-tool.exe"; Label = "Stable exe" },
    @{ Path = "C:\import\archive"; Label = "Archive map" },
    @{ Path = "C:\import-test"; Label = "Import-test root" },
    @{ Path = "C:\import-test\speel"; Label = "Import-test speel" },
    @{ Path = "C:\import-test\live"; Label = "Import-test live" },
    @{ Path = "C:\Users\Public\Desktop\BOM Import Tool.lnk"; Label = "Snelkoppeling" }
)

$allOk = $true
foreach ($check in $checks) {
    if (Test-Path $check.Path) {
        Write-Host "  [OK] $($check.Label): $($check.Path)" -ForegroundColor Green
    } else {
        Write-Host "  [!!] $($check.Label) ONTBREEKT: $($check.Path)" -ForegroundColor Red
        $allOk = $false
    }
}

Write-Host ""
if ($allOk) {
    Write-Host "Server setup voltooid!" -ForegroundColor Green
} else {
    Write-Host "Setup niet volledig — controleer de waarschuwingen hierboven." -ForegroundColor Red
    exit 1
}
```

Sla het script op als `scripts/setup-server-structure.ps1`.
  </action>
  <verify>
1. Controleer dat het bestand bestaat: `ls "c:/Users/rik/ritco.be/Ritco BV/Spinnekop/scripts/setup-server-structure.ps1"`
2. Controleer dat het script de juiste paden bevat:
   - `grep "C:\\import\\bom-import-tool.exe" scripts/setup-server-structure.ps1`
   - `grep "Public\\Desktop" scripts/setup-server-structure.ps1`
   - `grep "C:\\import-test" scripts/setup-server-structure.ps1`
   - `grep "C:\\import\\archive" scripts/setup-server-structure.ps1`
3. PowerShell syntax check (lokaal, zonder uitvoeren op server):
   `pwsh -NoProfile -Command "Get-Content 'scripts/setup-server-structure.ps1' | Out-Null; Write-Host 'Syntax OK'"`
  </verify>
  <done>
- setup-server-structure.ps1 bestaat in scripts/
- Script bevat alle 5 stappen (archive, import-test, migratie, opruimen, shortcut)
- Script is idempotent (veilig opnieuw uitvoeren)
- Eindigde met verificatie-sectie die alle paden checkt
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Voer setup-script uit op VMSERVERRUM via RDP</name>
  <files>N/A — uitgevoerd op server via RDP</files>
  <action>Kopieer setup-server-structure.ps1 naar Z: (de gedeelde import-map) zodat het bereikbaar is op VMSERVERRUM. Daarna verifieer VPN-connectiviteit, maak Z: drive aan via connect-ridderiq.ps1, en instrueer de gebruiker om via RDP in te loggen en het script als Administrator uit te voeren.</action>
  <verify>Alle 6 verificatiechecks in de script-output tonen [OK]. Desktop shortcut bestaat en opent de tool.</verify>
  <done>Server heeft correcte mappenstructuur, exe op stable locatie, shortcut op Public Desktop.</done>
  <what-built>setup-server-structure.ps1 script dat klaarstaat in scripts/</what-built>
  <how-to-verify>
**Voorbereiding (Claude doet dit):**
Claude kopieert het script naar Z: via de connect-ridderiq.ps1 workflow zodat het bereikbaar is op de server.

**Jij doet het volgende:**

1. Controleer VPN connectiviteit:
   - Ping VMSERVERRUM: `ping 10.0.1.5`
   - Als ping faalt: OpenVPN Connect controleren en opnieuw verbinden

2. Map Z: drive (als nog niet gedaan):
   - Voer uit: `powershell -File "c:\Users\rik\ritco.be\Ritco BV\Spinnekop\scripts\connect-ridderiq.ps1"`
   - Controleer dat Z: beschikbaar is in File Explorer

3. RDP naar VMSERVERRUM:
   - Open Remote Desktop Connection
   - Server: 10.0.1.5 (of VMSERVERRUM)
   - Login: ridderadmin

4. Voer het script uit in een PowerShell venster als Administrator op de server:
   ```
   powershell -ExecutionPolicy Bypass -File "Z:\setup-server-structure.ps1"
   ```

5. Controleer de output:
   - Alle 6 regels in de Verificatie-sectie moeten [OK] tonen
   - Geen rode [!!] foutmeldingen

6. Bevestig op de server:
   - Desktop toont "BOM Import Tool" snelkoppeling
   - Dubbelklik de snelkoppeling — tool opent met title "BOM Import Tool v1.0.0 [Speelomgeving]"

**Type "klaar" als alles [OK] is, of beschrijf welke stappen [!!] toonden.**
  </how-to-verify>
  <resume-signal>Type "klaar" als setup geslaagd is, of beschrijf de fout om te debuggen.</resume-signal>
</task>

</tasks>

<verification>
Na de checkpoint:
1. Server heeft `C:\import\bom-import-tool.exe` (stable locatie)
2. Server heeft `C:\import\archive\` (leeg, klaar voor eerste release)
3. Server heeft `C:\import-test\` met `speel\` en `live\` submappen
4. `C:\import\bom-import-tool\` map bestaat niet meer (of is leeg)
5. `C:\Users\Public\Desktop\BOM Import Tool.lnk` bestaat en opent de exe
6. Alle RDP-gebruikers (Evy en Rik) zien de snelkoppeling op het gedeelde bureaublad
</verification>

<success_criteria>
- scripts/setup-server-structure.ps1 bestaat lokaal
- Script succesvol uitgevoerd op VMSERVERRUM (alle verificatiechecks [OK])
- Evy heeft een snelkoppeling op het Public Desktop die direct opent zonder navigeren
- Stable exe op C:\import\bom-import-tool.exe (niet in submap)
- Dev-locatie C:\import-test\ klaar voor Phase 2
- Archive-locatie C:\import\archive\ klaar voor Phase 3
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/01-foundation/01-02-SUMMARY.md` aan.
</output>
