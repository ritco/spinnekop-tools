---
phase: 13-build-deploy
plan: "03"
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - scripts/audit_logger.py
  - scripts/eplan_converter.py
  - scripts/setup-spinnekoptools.sql
autonomous: true
requirements: [LOG-01]

must_haves:
  truths:
    - "Na een ePlan import (dry_run=False of dry_run=True) staat er een rij in EPLAN_IMPORT_LOG met de juiste aantallen"
    - "Als de SQL Server niet bereikbaar is, werkt de ePlan Import Tool gewoon verder zonder foutmelding"
    - "setup-spinnekoptools.sql kan twee keer achter elkaar worden uitgevoerd zonder fouten"
  artifacts:
    - path: "scripts/audit_logger.py"
      provides: "log_eplan_import() functie"
      contains: "log_eplan_import"
    - path: "scripts/eplan_converter.py"
      provides: "Audit logging call in convert()"
      contains: "log_eplan_import"
    - path: "scripts/setup-spinnekoptools.sql"
      provides: "EPLAN_IMPORT_LOG tabel DDL"
      contains: "EPLAN_IMPORT_LOG"
  key_links:
    - from: "scripts/eplan_converter.py"
      to: "scripts/audit_logger.py"
      via: "import + aanroep log_eplan_import() aan het einde van convert()"
      pattern: "log_eplan_import"
    - from: "scripts/audit_logger.py"
      to: "SpinnekopTools.EPLAN_IMPORT_LOG"
      via: "get_audit_connection() + INSERT"
      pattern: "INSERT INTO EPLAN_IMPORT_LOG"
---

<objective>
Voeg audit logging toe voor ePlan imports — net zoals BOM imports al gelogd worden naar SpinnekopTools.BOM_IMPORT_LOG.

Purpose: Florian/Toby's ePlan conversies worden centraal bijgehouden zodat je kunt zien wie wat importeerde, hoeveel artikelen werden gematch of aangemaakt, en of er fouten waren.

Output:
- `log_eplan_import()` functie in audit_logger.py (naast bestaande `log_import()`)
- `EPLAN_IMPORT_LOG` tabel DDL in setup-spinnekoptools.sql
- Aanroep in eplan_converter.py `convert()` — faalveilig, blokkeert nooit
</objective>

<execution_context>
@C:/Users/rik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/audit_logger.py
@scripts/eplan_converter.py
@scripts/setup-spinnekoptools.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voeg EPLAN_IMPORT_LOG toe aan audit_logger.py en setup-spinnekoptools.sql</name>
  <files>scripts/audit_logger.py, scripts/setup-spinnekoptools.sql</files>
  <action>
**audit_logger.py** — voeg toe onder de bestaande `log_import()` functie:

1. Voeg een tweede inline CREATE TABLE SQL constant toe (module-level, naast `_CREATE_TABLE_SQL`):

```python
_CREATE_EPLAN_TABLE_SQL = """
IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = 'EPLAN_IMPORT_LOG')
CREATE TABLE EPLAN_IMPORT_LOG (
    id                  INT IDENTITY PRIMARY KEY,
    tijdstip            DATETIME2        NOT NULL,
    gebruiker           NVARCHAR(100)    NOT NULL,
    toestel             NVARCHAR(100)    NOT NULL,
    omgeving            NVARCHAR(50)     NOT NULL,
    excel_bestand       NVARCHAR(500),
    project_naam        NVARCHAR(200),
    matched             INT,
    nieuwe_artikelen    INT,
    overgeslagen        INT,
    fouten              INT,
    duur_seconden       FLOAT,
    status              NVARCHAR(20)     NOT NULL,
    foutmelding         NVARCHAR(MAX)
);
"""
```

2. Voeg een tweede `_schema_initialized`-achtige vlag toe:
```python
_eplan_schema_initialized = False
```

3. Voeg een interne helper toe `_ensure_eplan_schema(conn)` identiek aan `_ensure_schema()` maar gebruikt `_CREATE_EPLAN_TABLE_SQL` en `_eplan_schema_initialized`.

4. Voeg de publieke functie toe:

```python
def log_eplan_import(
    *,
    omgeving: str,
    status: str,
    excel_bestand: str = None,
    project_naam: str = None,
    matched: int = None,
    nieuwe_artikelen: int = None,
    overgeslagen: int = None,
    fouten: int = None,
    duur_seconden: float = None,
    foutmelding: str = None,
    gebruiker: str = None,
    toestel: str = None,
):
```

De implementatie volgt exact het patroon van `log_import()`:
- `try/except Exception` als buitenste wrapper (faalveilig)
- `from app_config import get_audit_connection` binnen de try
- `conn, info = get_audit_connection()` — `None` check met `log.debug`
- `_ensure_eplan_schema(conn)` aanroepen
- auto-detect `gebruiker` via `os.getlogin()` / `USERNAME` env var
- auto-detect `toestel` via `socket.gethostname()`
- INSERT met alle kolommen, `conn.commit()`, `conn.close()`
- `log.debug("Audit log geschreven: ePlan %s → %s", omgeving, status)`
- Op exception: `log.warning("Audit logging mislukt (niet kritiek): %s", e)` + schrijf naar `audit-debug.log` (zelfde pad-logica als `log_import`)

INSERT SQL:
```sql
INSERT INTO EPLAN_IMPORT_LOG (
    tijdstip, gebruiker, toestel, omgeving,
    excel_bestand, project_naam, matched, nieuwe_artikelen,
    overgeslagen, fouten, duur_seconden, status, foutmelding
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
```

**setup-spinnekoptools.sql** — voeg toe na het bestaande `GRANT INSERT, SELECT ON BOM_IMPORT_LOG TO ridderadmin;` blok en voor het `-- Verificatie` blok:

```sql
IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = 'EPLAN_IMPORT_LOG')
CREATE TABLE EPLAN_IMPORT_LOG (
    id                  INT IDENTITY PRIMARY KEY,
    tijdstip            DATETIME2        NOT NULL,
    gebruiker           NVARCHAR(100)    NOT NULL,
    toestel             NVARCHAR(100)    NOT NULL,
    omgeving            NVARCHAR(50)     NOT NULL,
    excel_bestand       NVARCHAR(500),
    project_naam        NVARCHAR(200),
    matched             INT,
    nieuwe_artikelen    INT,
    overgeslagen        INT,
    fouten              INT,
    duur_seconden       FLOAT,
    status              NVARCHAR(20)     NOT NULL,
    foutmelding         NVARCHAR(MAX)
);
GO

GRANT INSERT, SELECT ON EPLAN_IMPORT_LOG TO ridderadmin;
GO
```

En voeg toe aan het Verificatie blok onderaan:
```sql
SELECT 'Tabel aangemaakt' AS status, name FROM sys.tables WHERE name = 'EPLAN_IMPORT_LOG';
```
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'scripts'); import audit_logger; assert hasattr(audit_logger, 'log_eplan_import'), 'log_eplan_import ontbreekt'; print('OK: log_eplan_import aanwezig')"</automated>
    <manual>Controleer dat setup-spinnekoptools.sql de string 'EPLAN_IMPORT_LOG' twee keer bevat: eenmalig in CREATE TABLE en eenmalig in GRANT.</manual>
  </verify>
  <done>
    - `audit_logger.log_eplan_import` is importeerbaar zonder fout
    - `setup-spinnekoptools.sql` bevat `EPLAN_IMPORT_LOG` CREATE TABLE + GRANT + verificatieregel
    - Beide bestanden zijn idempotent (IF NOT EXISTS patroon)
  </done>
</task>

<task type="auto">
  <name>Task 2: Roep log_eplan_import() aan in eplan_converter.convert()</name>
  <files>scripts/eplan_converter.py</files>
  <action>
In `eplan_converter.py`, aan het einde van de `convert()` functie — na het bestaande `log.info("Convert klaar: ...")` statement en vóór `return result`:

1. Voeg de import toe bovenaan het bestand (na de bestaande `try/except` app_config import blok):

```python
try:
    from audit_logger import log_eplan_import as _log_eplan_import
except ImportError:
    _log_eplan_import = None
```

Gebruik een `_log_eplan_import` alias zodat de naam niet botst met een lokale variabele.

2. Voeg de aanroep toe aan het einde van `convert()`, direct voor `return result`:

```python
    # Audit logging — faalveilig, blokkeert nooit
    if _log_eplan_import is not None:
        try:
            _log_eplan_import(
                omgeving=env,
                status='fout' if result.has_blockers else 'ok',
                excel_bestand=Path(excel_path).name,
                project_naam=result.projectnaam or None,
                matched=len(result.matched),
                nieuwe_artikelen=len(result.new_items),
                overgeslagen=len(result.skipped),
                fouten=len(result.errors),
                foutmelding='; '.join(result.errors) if result.errors else None,
            )
        except Exception as _e:
            log.warning("Audit logging mislukt (niet kritiek): %s", _e)
```

Let op: `duur_seconden` wordt niet meegegeven (geen timer in convert() — laat None). Dit is prima, de kolom is nullable.

De aanroep gaat ALTIJD, zowel bij dry_run=True als dry_run=False — beide runs zijn informatief.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'scripts'); import ast; src = open('scripts/eplan_converter.py', encoding='utf-8').read(); assert 'log_eplan_import' in src, 'aanroep ontbreekt in eplan_converter.py'; print('OK: log_eplan_import aanwezig in eplan_converter.py')"</automated>
    <manual>Verifieer visueel in eplan_converter.py dat de aanroep staat na het log.info("Convert klaar:...") statement en voor return result.</manual>
  </verify>
  <done>
    - `eplan_converter.py` importeert `log_eplan_import` faalveilig (try/except bij import)
    - Aanroep staat aan het einde van `convert()` voor `return result`
    - Status is 'fout' als `result.has_blockers`, anders 'ok'
    - Aanroep is zelf ook in try/except gewikkeld zodat een audit-fout nooit de converter blokkeert
  </done>
</task>

</tasks>

<verification>
Na beide taken:

```bash
python -c "
import sys
sys.path.insert(0, 'scripts')

# Test 1: audit_logger importeerbaar en heeft log_eplan_import
import audit_logger
assert hasattr(audit_logger, 'log_eplan_import'), 'FAIL: log_eplan_import ontbreekt'
print('PASS: audit_logger.log_eplan_import aanwezig')

# Test 2: eplan_converter importeerbaar (geen syntax fouten)
import importlib.util
spec = importlib.util.spec_from_file_location('eplan_converter', 'scripts/eplan_converter.py')
mod = importlib.util.module_from_spec(spec)
# Niet uitvoeren (vereist openpyxl/pyodbc) maar wel parsen
import ast
src = open('scripts/eplan_converter.py', encoding='utf-8').read()
ast.parse(src)
print('PASS: eplan_converter.py syntactisch correct')

# Test 3: aanroep aanwezig in eplan_converter
assert 'log_eplan_import' in src, 'FAIL: aanroep ontbreekt'
print('PASS: log_eplan_import aanroep aanwezig in eplan_converter')

# Test 4: setup SQL bevat beide tabellen
sql = open('scripts/setup-spinnekoptools.sql', encoding='utf-8').read()
assert 'EPLAN_IMPORT_LOG' in sql, 'FAIL: EPLAN_IMPORT_LOG ontbreekt in SQL'
assert 'BOM_IMPORT_LOG' in sql, 'FAIL: BOM_IMPORT_LOG verdwenen uit SQL'
print('PASS: setup-spinnekoptools.sql bevat beide tabellen')

print('Alle checks geslaagd.')
"
```
</verification>

<success_criteria>
- `audit_logger.log_eplan_import()` bestaat en is importeerbaar
- Aanroep staat in `eplan_converter.convert()` — na het log.info, voor return
- `setup-spinnekoptools.sql` bevat `EPLAN_IMPORT_LOG` DDL (IF NOT EXISTS) + GRANT + verificatieregel
- Dry-run conversies worden ook gelogd (status='ok' of 'fout')
- Als SQL Server onbereikbaar is: tool werkt gewoon door, geen exception omhoog
</success_criteria>

<output>
Na completion, maak `.planning/phases/13-build-deploy/13-03-SUMMARY.md` aan.

Update ook:
- `.planning/STATE.md`: last activity, metrics (plans completed: 14), decisions toevoegen als er keuzes gemaakt zijn
- `.planning/ROADMAP.md`: Phase 13 plan list bijwerken met `13-03-PLAN.md`
</output>
