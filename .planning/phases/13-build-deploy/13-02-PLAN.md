---
phase: 13-build-deploy
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - scripts/dist/eplan-import-tool.exe
autonomous: false
requirements:
  - BUILD-01
  - BUILD-02

must_haves:
  truths:
    - "eplan-import-tool.exe start op een machine zonder Python installatie en toont de GUI"
    - "Bij opstart controleert de tool of er een update beschikbaar is via GitHub (tag prefix eplan-)"
    - "GitHub release eplan-v1.0.0 is aangemaakt met eplan-import-tool.exe als asset"
  artifacts:
    - path: "scripts/dist/eplan-import-tool.exe"
      provides: "Standalone executable voor ePlan Import Tool"
      min_lines: 0
  key_links:
    - from: "eplan_main.py"
      to: "app_config.check_for_update"
      via: "check_for_update('eplan-import-tool', __version__)"
      pattern: "eplan-import-tool"
    - from: "app_config._fetch_github_release"
      to: "GitHub releases API"
      via: "tag startswith 'eplan-'"
      pattern: "eplan-"
---

<objective>
Bouw de ePlan Import Tool exe via PyInstaller, maak de eerste GitHub release aan, en verifieer dat de exe werkt op een machine zonder Python.

Purpose: De tool werkend afleveren voor Florian/Toby — installeerbaar via GitHub release pipeline zonder Python kennis.
Output: scripts/dist/eplan-import-tool.exe + GitHub release eplan-v1.0.0 (of hogere patch)
</objective>

<execution_context>
@C:/Users/rik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-build-deploy/13-01-SUMMARY.md

# Referentiebestanden
@scripts/eplan_main.py
@workflows/tool-release-pipeline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build eplan-import-tool.exe via PyInstaller en maak GitHub release</name>
  <files>scripts/dist/eplan-import-tool.exe</files>
  <action>
Voer de PyInstaller build uit vanuit de scripts/ directory:

```bash
cd "c:/Users/rik/ritco.be/Ritco BV/Spinnekop/scripts"
python -m PyInstaller --clean eplan-import-tool.spec
```

Als de build slaagt: `scripts/dist/eplan-import-tool.exe` bestaat en heeft een redelijke bestandsgrootte (verwacht >10 MB vanwege customtkinter).

Als de build faalt met een hiddenimports fout:
- Lees de foutmelding en voeg de ontbrekende module toe aan hiddenimports in eplan-import-tool.spec
- Overweeg ook 'openpyxl' toe te voegen — eplan_converter.py importeert openpyxl via een try/except
- Herstart de build

Na succesvolle build: maak de eerste GitHub release aan:

```bash
cd "c:/Users/rik/ritco.be/Ritco BV/Spinnekop"
gh release create eplan-v1.0.0 \
  "scripts/dist/eplan-import-tool.exe" \
  --repo ritco/spinnekop-tools \
  --title "ePlan Import Tool v1.0.0" \
  --notes "Eerste release van de ePlan Import Tool. Vervangt handmatig Excel-verwerken voor ePlan stuklijst-imports naar RidderIQ."
```

Verificeer dat de release aangemaakt is:
```bash
gh release view eplan-v1.0.0 --repo ritco/spinnekop-tools
```

KRITIEK voor auto-update: de tag moet beginnen met `eplan-`. In app_config.py geldt:
`tool_prefix = tool_name.split('-')[0] + '-'` waarbij tool_name='eplan-import-tool' → prefix='eplan-'.
Tag `eplan-v1.0.0` wordt daarmee correct opgepikt bij de volgende update check.
  </action>
  <verify>
    <automated>cd "c:/Users/rik/ritco.be/Ritco BV/Spinnekop" && python -c "
from pathlib import Path
exe = Path('scripts/dist/eplan-import-tool.exe')
assert exe.exists(), f'exe niet gevonden: {exe}'
size_mb = exe.stat().st_size / (1024*1024)
assert size_mb > 5, f'exe te klein ({size_mb:.1f} MB) — build mislukt?'
print(f'OK: exe aanwezig ({size_mb:.1f} MB)')
" && gh release view eplan-v1.0.0 --repo ritco/spinnekop-tools --json tagName,assets --jq ".tagName"</automated>
    <manual>Controleer op https://github.com/ritco/spinnekop-tools/releases dat eplan-v1.0.0 zichtbaar is met eplan-import-tool.exe als asset.</manual>
  </verify>
  <done>scripts/dist/eplan-import-tool.exe bestaat (>5 MB) en GitHub release eplan-v1.0.0 is aangemaakt met de exe als downloadbaar asset.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifieer exe start zonder Python</name>
  <files>scripts/dist/eplan-import-tool.exe</files>
  <action>
Gebruiker verifieert dat de gebouwde exe correct opstart. Dit is de enige verificatie die niet geautomatiseerd kan worden: alleen een mens kan bevestigen dat de GUI zichtbaar is en correct werkt.
  </action>
  <verify>
    <automated>MISSING — visuele verificatie vereist, zie how-to-verify hieronder</automated>
    <manual>
Stap 1: Kopieer scripts/dist/eplan-import-tool.exe naar een tijdelijke map (bijv. C:\Temp\eplan-test\)
Stap 2: Dubbelklik op eplan-import-tool.exe (ZONDER dat Python geinstalleerd hoeft te zijn)
Stap 3: Controleer dat:
  - ePlan Import Tool venster opent (900x700 px, blauwe header "ePlan Import Tool — Spinnekop")
  - Versienummer v1.0.0 zichtbaar in de header
  - Omgeving selector (Speelomgeving / Live) zichtbaar
  - "Open ePlan Excel" knop aanwezig
  - Geen Python crash of DLL fout popup
Stap 4: Wacht 10 seconden — er mag GEEN update-dialoog verschijnen (v1.0.0 is nieuwste)
Stap 5: Sluit de tool via het kruisje
Bij problemen: "Failed to execute script" = ontbrekende hiddenimport, noteer de module naam
    </manual>
  </verify>
  <done>Gebruiker bevestigt dat de GUI opent zonder Python installatie en zonder crash.</done>
  <what-built>
eplan-import-tool.exe gebouwd via PyInstaller en gepubliceerd als GitHub release eplan-v1.0.0.
Alle dependencies gebundeld: customtkinter, openpyxl, pyodbc, app_config, eplan_gui, eplan_converter, history.
  </what-built>
  <how-to-verify>
Stap 1: Kopieer scripts/dist/eplan-import-tool.exe naar C:\Temp\eplan-test\
Stap 2: Dubbelklik op de exe
Stap 3: Controleer GUI (header, versie, omgeving selector, Open-knop, geen crash)
Stap 4: Wacht 10 sec — geen update dialoog verwacht
Stap 5: Sluit via kruisje
  </how-to-verify>
  <resume-signal>Typ "approved" als de GUI correct opent, of beschrijf de foutmelding als er iets misgaat.</resume-signal>
</task>

</tasks>

<verification>
Na checkpoint goedkeuring:
1. `scripts/dist/eplan-import-tool.exe` bestaat en is >5 MB
2. GitHub release `eplan-v1.0.0` op ritco/spinnekop-tools heeft `eplan-import-tool.exe` als asset
3. Exe start zonder Python installatie (menselijk geverifieerd)
4. Geen valse update dialoog bij v1.0.0 (update check werkt correct)
</verification>

<success_criteria>
- BUILD-01: exe start op machine zonder Python en toont GUI zonder fouten (checkpoint goedgekeurd)
- BUILD-02: tag prefix eplan- correct — auto-update pikt toekomstige eplan-v1.x.x releases op
- GitHub release aangemaakt en asset downloadbaar via ritco/spinnekop-tools
</success_criteria>

<output>
Na completion, maak `.planning/phases/13-build-deploy/13-02-SUMMARY.md` aan met:
- Build resultaat (exe grootte, eventuele extra hiddenimports)
- GitHub release URL
- Checkpoint resultaat

Update `.claude/memory/build-deploy.md` met:
- ePlan build commando: `python -m PyInstaller --clean eplan-import-tool.spec`
- Tag prefix: eplan- (eplan-v1.0.0)
- Hiddenimports die uiteindelijk gebruikt zijn
</output>
