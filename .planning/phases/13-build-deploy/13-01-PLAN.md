---
phase: 13-build-deploy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eplan-import-tool.spec
  - scripts/deploy/eplan/install-live.ps1
  - scripts/deploy/eplan/install-test.ps1
  - scripts/deploy/eplan/README.md
  - workflows/tool-release-pipeline.md
autonomous: true
requirements:
  - BUILD-02
  - BUILD-03

must_haves:
  truths:
    - "PyInstaller kan de exe bouwen vanuit eplan-import-tool.spec zonder fouten"
    - "install-live.ps1 en install-test.ps1 bevatten een ping-check op 10.0.1.5 voor het kopieren"
    - "tool-release-pipeline.md documenteert de ePlan tool als derde tool in de tabel"
  artifacts:
    - path: "scripts/eplan-import-tool.spec"
      provides: "PyInstaller build definitie voor eplan-import-tool.exe"
      contains: "eplan_main.py"
    - path: "scripts/deploy/eplan/install-live.ps1"
      provides: "Installatiescript voor live server share (Z:)"
      contains: "ping"
    - path: "scripts/deploy/eplan/install-test.ps1"
      provides: "Installatiescript voor test server share (Y:)"
      contains: "ping"
    - path: "scripts/deploy/eplan/README.md"
      provides: "Korte deploy-instructies"
    - path: "workflows/tool-release-pipeline.md"
      provides: "Bijgewerkte tool-tabel met ePlan tool"
  key_links:
    - from: "scripts/eplan-import-tool.spec"
      to: "scripts/eplan_main.py"
      via: "Analysis entry point"
      pattern: "eplan_main\\.py"
    - from: "scripts/deploy/eplan/install-live.ps1"
      to: "10.0.1.5"
      via: "ping check before copy"
      pattern: "ping.*10\\.0\\.1\\.5"
---

<objective>
Bouw de PyInstaller spec en installatiescripts voor de ePlan Import Tool klaar — zodat Plan 02 direct kan builden en releasen.

Purpose: Alle configuratie-artefacten gereed hebben voordat de daadwerkelijke build en GitHub release plaatsvinden.
Output: eplan-import-tool.spec, install-live.ps1, install-test.ps1, README.md, bijgewerkt tool-release-pipeline.md
</objective>

<execution_context>
@C:/Users/rik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Referentiebestanden
@scripts/bom-import-tool.spec
@scripts/eplan_main.py
@scripts/eplan_gui.py
@scripts/eplan_converter.py
@scripts/app_config.py
@workflows/tool-release-pipeline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maak eplan-import-tool.spec</name>
  <files>scripts/eplan-import-tool.spec</files>
  <action>
Maak scripts/eplan-import-tool.spec op basis van scripts/bom-import-tool.spec.

Wijzigingen t.o.v. bom-import-tool.spec:
- Entry point: ['eplan_main.py'] (was ['main.py'])
- name: 'eplan-import-tool' (was 'bom-import-tool')
- hiddenimports: ['customtkinter', 'eplan_gui', 'eplan_converter', 'history', 'app_config']
  - VERWIJDER: 'gui', 'bom_converter', 'validation_engine', 'sql_validator', 'audit_logger'
  - VOEG TOE: 'eplan_gui', 'eplan_converter'
  - BEHOUD: 'customtkinter', 'history', 'app_config'

Alle andere opties (debug=False, console=False, upx=True, strip=False, etc.) blijven identiek aan bom-import-tool.spec.

Opmerking: geen GitHub Actions workflow nodig — release pipeline is volledig manueel via gh CLI (zie tool-release-pipeline.md). Geen .github/workflows/ bestand aanmaken.
  </action>
  <verify>
    <automated>cd "c:/Users/rik/ritco.be/Ritco BV/Spinnekop/scripts" && python -c "
import ast, sys
with open('eplan-import-tool.spec') as f:
    content = f.read()
assert 'eplan_main.py' in content, 'Entry point ontbreekt'
assert 'eplan-import-tool' in content, 'Exe naam ontbreekt'
assert 'eplan_gui' in content, 'eplan_gui hiddenimport ontbreekt'
assert 'eplan_converter' in content, 'eplan_converter hiddenimport ontbreekt'
assert 'bom_converter' not in content, 'bom_converter mag niet in spec'
assert 'validation_engine' not in content, 'validation_engine mag niet in spec'
print('OK: spec correct')
"</automated>
  </verify>
  <done>eplan-import-tool.spec bevat correcte entry point, exe naam en hiddenimports. Geen BOM tool module-namen aanwezig.</done>
</task>

<task type="auto">
  <name>Task 2: Maak install-live.ps1 en install-test.ps1</name>
  <files>
    scripts/deploy/eplan/install-live.ps1
    scripts/deploy/eplan/install-test.ps1
    scripts/deploy/eplan/README.md
  </files>
  <action>
Maak scripts/deploy/eplan/ map aan en schrijf de twee installatiescripts.

**install-live.ps1** — kopieert exe naar Z: (live server share, \\10.0.1.5\import):
```powershell
# ePlan Import Tool — Installatie op live server share
# Gebruik: PowerShell -ExecutionPolicy Bypass -File install-live.ps1

$Server = "10.0.1.5"
$SharePath = "\\$Server\import"
$ExeName = "eplan-import-tool.exe"
$LocalExe = Join-Path $PSScriptRoot "..\..\dist\$ExeName"

# Ping check
Write-Host "Ping check op $Server..."
if (-not (Test-Connection -ComputerName $Server -Count 1 -Quiet -ErrorAction SilentlyContinue)) {
    Write-Error "Server $Server niet bereikbaar. Controleer VPN."
    exit 1
}

# Controleer lokale exe
if (-not (Test-Path $LocalExe)) {
    Write-Error "Exe niet gevonden: $LocalExe. Bouw eerst met PyInstaller."
    exit 1
}

# Kopieer naar share
Write-Host "Kopieren naar $SharePath..."
try {
    Copy-Item -Path $LocalExe -Destination $SharePath -Force
    Write-Host "OK: $ExeName geinstalleerd op $SharePath"
} catch {
    Write-Error "Kopieren mislukt: $_"
    exit 1
}
```

**install-test.ps1** — identieke structuur, maar kopieert naar Y: (test share, \\10.0.1.5\import-test):
- Zelfde ping-check op 10.0.1.5
- $SharePath = "\\$Server\import-test"
- Write-Host berichten vermelden "test server share"

**README.md** — kort deploy-draaiboek:
```markdown
# ePlan Import Tool — Deploy

## Bouwen
cd scripts/
python -m PyInstaller --clean eplan-import-tool.spec
# Output: scripts/dist/eplan-import-tool.exe

## Installeren op test share (Y:)
PowerShell -ExecutionPolicy Bypass -File scripts/deploy/eplan/install-test.ps1

## Installeren op live share (Z:)
PowerShell -ExecutionPolicy Bypass -File scripts/deploy/eplan/install-live.ps1

## GitHub release aanmaken
gh release create eplan-v1.0.0 "scripts/dist/eplan-import-tool.exe" \
  --repo ritco/spinnekop-tools \
  --title "ePlan Import Tool v1.0.0" \
  --notes "Eerste release"

Zie workflows/tool-release-pipeline.md voor volledig draaiboek.
```

Let op: de scripts gebruiken relatieve paden ($PSScriptRoot\..\..\dist\) zodat ze werken vanuit elke locatie. Ping via Test-Connection (niet ping.exe) voor betrouwbaarheid in PowerShell.
  </action>
  <verify>
    <automated>powershell -NonInteractive -Command "
\$live = 'c:/Users/rik/ritco.be/Ritco BV/Spinnekop/scripts/deploy/eplan/install-live.ps1'
\$test = 'c:/Users/rik/ritco.be/Ritco BV/Spinnekop/scripts/deploy/eplan/install-test.ps1'
\$readme = 'c:/Users/rik/ritco.be/Ritco BV/Spinnekop/scripts/deploy/eplan/README.md'
if (-not (Test-Path \$live)) { Write-Error 'install-live.ps1 ontbreekt'; exit 1 }
if (-not (Test-Path \$test)) { Write-Error 'install-test.ps1 ontbreekt'; exit 1 }
if (-not (Test-Path \$readme)) { Write-Error 'README.md ontbreekt'; exit 1 }
\$liveContent = Get-Content \$live -Raw
\$testContent = Get-Content \$test -Raw
if (\$liveContent -notmatch '10\.0\.1\.5') { Write-Error 'live: server IP ontbreekt'; exit 1 }
if (\$testContent -notmatch '10\.0\.1\.5') { Write-Error 'test: server IP ontbreekt'; exit 1 }
if (\$liveContent -notmatch 'Test-Connection') { Write-Error 'live: ping check ontbreekt'; exit 1 }
if (\$testContent -notmatch 'Test-Connection') { Write-Error 'test: ping check ontbreekt'; exit 1 }
if (\$liveContent -notmatch 'import-test' -and \$liveContent -match 'import-test') { Write-Error 'live verwijst naar test share'; exit 1 }
if (\$testContent -notmatch 'import-test') { Write-Error 'test: import-test share ontbreekt'; exit 1 }
Write-Host 'OK: install scripts aanwezig met ping-check'
"</automated>
  </verify>
  <done>install-live.ps1 en install-test.ps1 bestaan met ping-check op 10.0.1.5, kopieren naar respectievelijk \import en \import-test share. README.md bevat build en deploy commando's.</done>
</task>

<task type="auto">
  <name>Task 3: Update tool-release-pipeline.md</name>
  <files>workflows/tool-release-pipeline.md</files>
  <action>
Voeg de ePlan Import Tool toe aan de bestaande tabel in workflows/tool-release-pipeline.md.

Zoek de tabel onder "## Bestaande tools" (bevat BOM Import Tool en Productiestructuur). Voeg een derde rij toe:

| Tool | exe-naam | Tag-prefix | Entry point |
|------|----------|------------|-------------|
| BOM Import Tool | `bom-import-tool.exe` | `bom-` | `scripts/main.py` |
| Productiestructuur | `productiestructuur.exe` | `productiestructuur-` | `scripts/phantom_tool.py` |
| **ePlan Import Tool** | `eplan-import-tool.exe` | `eplan-` | `scripts/eplan_main.py` |

Voeg ook toe onder "## Naamconventies" de ePlan-patronen:

| `eplan-v1.0.0` | ePlan Import Tool | Stabiel (live gebruikers) |
| `eplan-v1.0.0-test` | ePlan Import Tool | Prerelease (testomgeving) |

Geen andere wijzigingen aan het bestand.
  </action>
  <verify>
    <automated>python -c "
with open('c:/Users/rik/ritco.be/Ritco BV/Spinnekop/workflows/tool-release-pipeline.md', encoding='utf-8') as f:
    content = f.read()
assert 'eplan-import-tool.exe' in content, 'ePlan exe naam ontbreekt'
assert 'eplan_main.py' in content, 'ePlan entry point ontbreekt'
assert 'eplan-' in content, 'eplan- tag prefix ontbreekt'
print('OK: tool-release-pipeline.md bijgewerkt')
"</automated>
  </verify>
  <done>tool-release-pipeline.md bevat de ePlan Import Tool in de bestaande tools tabel en naamconventies tabel.</done>
</task>

</tasks>

<verification>
Na afronding van alle taken:
1. `scripts/eplan-import-tool.spec` bevat `eplan_main.py` als entry point en `eplan-import-tool` als naam
2. `scripts/deploy/eplan/` bevat install-live.ps1, install-test.ps1 en README.md
3. Beide install scripts bevatten Test-Connection ping-check op 10.0.1.5
4. `workflows/tool-release-pipeline.md` vermeldt de ePlan tool
</verification>

<success_criteria>
- PyInstaller spec klaar voor build (Task 2 in Plan 02 kan direct `python -m PyInstaller --clean eplan-import-tool.spec` uitvoeren)
- Install scripts aanwezig met ping-check
- Tool gedocumenteerd in release pipeline workflow
</success_criteria>

<output>
Na completion, maak `.planning/phases/13-build-deploy/13-01-SUMMARY.md` aan met:
- Welke bestanden aangemaakt zijn
- hiddenimports die gebruikt zijn in de spec
- Eventuele afwijkingen van de bom-import-tool.spec structuur
</output>
