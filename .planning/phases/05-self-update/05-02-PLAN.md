---
phase: 05-self-update
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/main.py
autonomous: false
requirements:
  - UPD-01
  - UPD-02
  - UPD-03
  - UPD-04

must_haves:
  truths:
    - "Evy ziet een update dialog wanneer er een nieuwere versie op de share staat"
    - "Na klikken op Updaten herstart de tool met de nieuwe versie"
    - "Als Evy niet op het netwerk zit, start de tool gewoon op"
  artifacts:
    - path: "scripts/dist/bom-import-tool.exe"
      provides: "Gebouwde exe met self-update logica"
    - path: "scripts/main.py"
      provides: "Versie verhoogd naar 1.2.1 voor update test"
      contains: "__version__"
  key_links:
    - from: "scripts/dist/bom-import-tool.exe"
      to: "\\\\10.0.1.5\\import\\version.json"
      via: "check_for_update leest remote versie bij opstart"
      pattern: "check_for_update"
---

<objective>
Build de tool, bump de versie, promote naar Z:, en verifieer dat de volledige self-update cyclus werkt end-to-end.

Purpose: De code-wijzigingen uit Plan 01 moeten bewezen worden in de echte omgeving: exe bouwen, naar de server deployen, en bevestigen dat een oudere lokale kopie de update correct detecteert, toont, en uitvoert.

Output: Werkende self-update flow bevestigd door user, versie 1.2.1 op Z: met bijgewerkte version.json.
</objective>

<execution_context>
@C:/Users/rik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-self-update/05-01-SUMMARY.md
@scripts/main.py
@scripts/promote.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump versie naar 1.2.1, build exe, deploy naar Y:, promote naar Z:</name>
  <files>scripts/main.py</files>
  <action>
Volledige build-deploy-promote cyclus om de self-update te kunnen testen:

1. **Versie bump**: Wijzig `__version__ = '1.2.0'` naar `__version__ = '1.2.1'` in scripts/main.py
   - Dit is nodig zodat de tool op Z: versie 1.2.1 heeft, en een lokale kopie met 1.2.0 de update detecteert

2. **Build**: Run vanuit scripts/:
   ```
   python -m PyInstaller --clean bom-import-tool.spec
   ```
   Verifieer dat dist/bom-import-tool.exe bestaat en groter dan 10MB is.

3. **VPN + drive check**: Ping 10.0.1.5, map Y: en Z: drives via .tmp/map-y.ps1 en .tmp/map-z.ps1

4. **Deploy naar Y:**: Kopieer dist/bom-import-tool.exe naar Y: (import-test share)

5. **Promote naar Z:**: Run `powershell -ExecutionPolicy Bypass -File scripts/promote.ps1 -Tool bom`
   - Dit kopieert van Y: naar Z:, archiveert de oude versie, en update version.json
   - Verifieer dat version.json op Z: nu "bom-import-tool": "1.2.1" bevat

6. **Maak een test-kopie met oude versie**: Kopieer de HUIDIGE Z:\archive\ versie (1.2.0) naar een lokale testmap (.tmp/update-test/) zodat de user die kan draaien om de update dialog te zien.
   - Of: instrueer de user om de bestaande 1.2.0 exe die Evy heeft te gebruiken voor de test.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -Command "if (Test-Path 'Z:\version.json') { $v = Get-Content 'Z:\version.json' | ConvertFrom-Json; if ($v.'bom-import-tool' -eq '1.2.1') { Write-Output 'PASS: version.json has 1.2.1' } else { Write-Error \"Wrong version: $($v.'bom-import-tool')\" } } else { Write-Error 'version.json not found on Z:' }"</automated>
    <manual>Check Z:\bom-import-tool.exe exists and Z:\version.json shows 1.2.1</manual>
  </verify>
  <done>
    - __version__ in main.py is '1.2.1'
    - bom-import-tool.exe v1.2.1 staat op Z: (stable)
    - version.json op Z: bevat "bom-import-tool": "1.2.1"
    - Oude versie (1.2.0) gearchiveerd in Z:\archive\
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifieer self-update end-to-end</name>
  <files>n/a</files>
  <action>
User voert handmatige verificatie uit van de self-update mechaniek in drie scenario's: update beschikbaar, netwerk onbereikbaar, en geen update beschikbaar. Volledige self-update mechaniek: de tool checkt bij opstart version.json op \\10.0.1.5\import, toont een CTk update dialog als er een nieuwere versie is, en kopieert + herstart bij "Updaten".
  </action>
  <verify>
**Test 1: Update detectie en dialog (op het netwerk)**
1. Neem een kopie van bom-import-tool.exe v1.2.0 (uit Z:\archive\ of van Evy's laptop)
2. Zet die ergens lokaal neer (bijv. Desktop)
3. Dubbelklik om te starten
4. Verwacht: een update dialog verschijnt met "Huidige versie: v1.2.0" en "Nieuwe versie: v1.2.1" en een groene "Updaten" knop
5. Klik op "Updaten"
6. Verwacht: de tool sluit, er verschijnt kort een CMD venster, en de tool herstart
7. Verwacht: na herstart toont de titelbalk v1.2.1

**Test 2: Graceful fallback (NIET op het netwerk)**
1. Koppel VPN los (of gebruik een PC zonder netwerktoegang tot 10.0.1.5)
2. Start bom-import-tool.exe
3. Verwacht: tool start normaal op binnen 3-5 seconden, geen foutmelding, geen zichtbare vertraging

**Test 3: Geen update beschikbaar**
1. Start bom-import-tool.exe v1.2.1 (de versie op Z:) terwijl je op het netwerk zit
2. Verwacht: tool start direct op zonder update dialog (versies zijn gelijk)
  </verify>
  <done>
User bevestigt: (1) update dialog verschijnt bij oudere versie, (2) update + herstart werkt, (3) tool start snel zonder netwerk, (4) geen dialog bij gelijke versie.
  </done>
</task>

</tasks>

<verification>
1. version.json op Z: bevat "bom-import-tool": "1.2.1"
2. User bevestigt: update dialog verschijnt bij oudere lokale versie
3. User bevestigt: tool start normaal zonder netwerk
4. User bevestigt: geen dialog bij gelijke versie
</verification>

<success_criteria>
- Self-update cyclus werkt end-to-end: detectie, dialog, kopie, herstart (UPD-01, UPD-02, UPD-03)
- Graceful fallback bevestigd: geen fout of vertraging zonder netwerk (UPD-04)
- v1.2.1 staat live op Z: met correcte version.json
</success_criteria>

<output>
After completion, create `.planning/phases/05-self-update/05-02-SUMMARY.md`
</output>
