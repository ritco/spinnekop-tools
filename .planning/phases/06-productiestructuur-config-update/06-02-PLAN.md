---
phase: 06-productiestructuur-config-update
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/dist/productiestructuur.exe
autonomous: false
requirements: [PST-01, PST-02]

must_haves:
  truths:
    - "productiestructuur.exe v1.0.0 staat op Z: (stable share)"
    - "version.json op Z: bevat productiestructuur versie 1.0.0"
    - "productiestructuur.exe start op en toont v1.0.0 in de titelbalk"
    - "Bij een oudere lokale versie verschijnt de update dialog"
    - "Als de share niet bereikbaar is, start de tool gewoon op"
  artifacts:
    - path: "scripts/dist/productiestructuur.exe"
      provides: "Gecompileerde productiestructuur tool v1.0.0"
  key_links:
    - from: "Z:/productiestructuur.exe"
      to: "Z:/version.json"
      via: "promote.ps1 -Tool prod genereert version.json met nieuwe versie"
      pattern: "productiestructuur.*1\\.0\\.0"
---

<objective>
Build productiestructuur.exe v1.0.0, deploy naar Y:, promote naar Z:, en verifieer end-to-end dat config.json en self-update correct werken.

Purpose: Jurgen (en Rik voor testen) kan productiestructuur.exe v1.0.0 opstarten met config.json en self-update, identiek aan bom-import-tool.

Output: productiestructuur.exe v1.0.0 live op Z:, version.json bijgewerkt, E2E bewezen
</objective>

<execution_context>
@C:/Users/rik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-productiestructuur-config-update/06-01-SUMMARY.md
@scripts/phantom_tool.py
@scripts/productiestructuur.spec
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build productiestructuur.exe, deploy naar Y:, promote naar Z:</name>
  <files>scripts/dist/productiestructuur.exe</files>
  <action>
1. **Build**: Vanuit `scripts/` directory:
   ```
   python -m PyInstaller --clean productiestructuur.spec
   ```
   Controleer dat `scripts/dist/productiestructuur.exe` bestaat en groter is dan 5 MB.

2. **VPN en drive check**: Ping 10.0.1.5 om VPN te verifi&euml;ren. Map Y: en Z: drives via de .tmp/ scripts:
   ```
   ping -n 1 -w 2000 10.0.1.5
   powershell -ExecutionPolicy Bypass -File ".tmp/map-y.ps1"
   powershell -ExecutionPolicy Bypass -File ".tmp/map-z.ps1"
   ```

3. **Deploy naar Y:** (test):
   ```
   cp scripts/dist/productiestructuur.exe Y:/
   ```

4. **Promote naar Z:** via promote.ps1:
   ```
   powershell -ExecutionPolicy Bypass -File scripts/promote.ps1 -Tool prod
   ```
   Dit archiveert de huidige versie, kopieert van Y: naar Z:, en genereert een bijgewerkte version.json.

5. **Verificatie**:
   - Check dat `Z:/productiestructuur.exe` bestaat
   - Check dat `Z:/version.json` de key `productiestructuur` bevat met waarde `1.0.0`
   - Check dat `Z:/version.json` ook nog de bestaande `bom-import-tool` key bevat
  </action>
  <verify>
    <automated>ls -la "Z:/productiestructuur.exe" && python -c "import json; v=json.load(open('Z:/version.json','r',encoding='utf-8-sig')); assert v.get('productiestructuur')=='1.0.0', f'Got {v}'; assert 'bom-import-tool' in v; print('version.json OK:', v)"</automated>
    <manual>Controleer dat Z:/archive/ een backup bevat van de vorige productiestructuur versie (als die bestond)</manual>
  </verify>
  <done>productiestructuur.exe v1.0.0 staat op Z:, version.json bevat productiestructuur=1.0.0 en bom-import-tool versie is intact</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E verificatie productiestructuur config + self-update</name>
  <files>n/a</files>
  <action>
User voert handmatige verificatie uit van de productiestructuur tool: config.json integratie, self-update mechaniek, en graceful fallback. Dit bewijst dat PST-01 (config) en PST-02 (self-update) end-to-end werken in de productieomgeving.
  </action>
  <verify>
**Test 1: Eerste opstart zonder config.json**
1. Kopieer `Z:/productiestructuur.exe` naar een lokale map (bv. Desktop)
2. Dubbelklik op productiestructuur.exe
3. VERWACHT: Settings dialog opent automatisch (want geen config.json)
4. Vul de SQL instellingen in en klik "Opslaan"
5. VERWACHT: config.json verschijnt naast de exe

**Test 2: Normaal gebruik**
1. Start productiestructuur.exe opnieuw
2. VERWACHT: Titelbalk toont "Productiestructuur v1.0.0"
3. Kies Speel omgeving, vul een artikelcode in, klik Ophalen
4. VERWACHT: Boom verschijnt, phantom toggles werken

**Test 3: Self-update dialog (als mogelijk)**
Als je een oudere versie hebt (bv. 0.1.0) op een andere locatie:
1. Start de oude versie terwijl Z: bereikbaar is
2. VERWACHT: Update dialog verschijnt met "Nieuwe versie: v1.0.0"
3. Klik "Updaten" of "Later" om te verifieren dat beide knoppen werken

**Test 4: Graceful fallback zonder netwerk**
1. Verbreek VPN of ontkoppel netwerk
2. Start productiestructuur.exe
3. VERWACHT: Tool start normaal op binnen 5 seconden, geen foutmelding
  </verify>
  <done>
User bevestigt: (1) settings dialog opent bij ontbrekende config.json, (2) tool start op met v1.0.0 in titelbalk en werkt normaal, (3) update dialog verschijnt bij oudere versie, (4) tool start snel zonder netwerk.
  </done>
</task>

</tasks>

<verification>
- productiestructuur.exe v1.0.0 staat op Z:
- version.json op Z: bevat productiestructuur=1.0.0
- Tool start op, leest config.json, toont v1.0.0 in titelbalk
- Settings dialog opent bij ontbrekende config
- Self-update dialog verschijnt bij oudere lokale versie
- Graceful fallback bij onbereikbare share
</verification>

<success_criteria>
- v1.0.0 live op Z: met correcte version.json
- E2E goedgekeurd door gebruiker (config, self-update, fallback)
- Milestone v1.1 compleet: beide tools draaien met config.json en self-update
</success_criteria>

<output>
After completion, create `.planning/phases/06-productiestructuur-config-update/06-02-SUMMARY.md`
</output>
