---
phase: 11-core-converter
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - scripts/eplan_converter.py
autonomous: true
requirements:
  - BOM-01
  - BOM-02
  - BOM-03

must_haves:
  truths:
    - "convert(excel_path, 'speel', dry_run=False) schrijft 02-stuklijst-header.csv en 03-stuklijstregels.csv wanneer er geen blokkerende fouten zijn"
    - "Als er nieuwe artikelen zijn, wordt ook 01-nieuwe-artikelen-eplan.csv geschreven; als er geen nieuwe artikelen zijn wordt dit bestand NIET aangemaakt"
    - "Als de stuklijstcode al bestaat in R_ASSEMBLY, bevat ConversionResult.errors een blokkerende fout en worden er GEEN bestanden geschreven"
    - "convert() met dry_run=True schrijft nooit bestanden, ongeacht of er blokkerende fouten zijn"
    - "CSV-bestanden gebruiken semicolon als scheidingsteken en UTF-8-BOM encoding — identiek aan bom_converter.py"
    - "01-nieuwe-artikelen-eplan.csv heeft exact dezelfde kolomheaders als 01-artikelen.csv uit bom_converter.py"
    - "02-stuklijst-header.csv heeft exact dezelfde kolomheaders als 02-stuklijst-headers.csv uit bom_converter.py"
    - "03-stuklijstregels.csv heeft exact dezelfde kolomheaders als 03-stuklijstregels.csv uit bom_converter.py"
  artifacts:
    - path: "scripts/eplan_converter.py"
      provides: "Volledige headless converter met output-generatie"
      min_lines: 350
      exports:
        - "convert"
        - "write_output"
        - "ConversionResult"
  key_links:
    - from: "eplan_converter.write_output"
      to: "app_config.get_output_basis()"
      via: "output_dir = get_output_basis() / env"
      pattern: "get_output_basis"
    - from: "eplan_converter.convert"
      to: "R_ASSEMBLY"
      via: "SELECT COUNT(*) FROM R_ASSEMBLY WHERE CODE = ?"
      pattern: "R_ASSEMBLY.*CODE"
    - from: "01-nieuwe-artikelen-eplan.csv"
      to: "R_ITEM import schema"
      via: "Identieke headers als bom_converter.py generate_artikelen()"
      pattern: "Artikelcode.*Omschrijving.*Artikelgroep"
---

<objective>
Voltooi `eplan_converter.py` met output-generatie: stuklijst-duplicaatcheck, drie CSV-schrijvers, en een end-to-end integratietest.

Purpose: De converter is alleen bruikbaar als hij ook de juiste bestanden produceert in het juiste formaat. Phase 12 (GUI) kan pas gebouwd worden als convert() een betrouwbare ConversionResult teruggeeft en dry_run=False de verwachte bestanden schrijft.

Output: Volledige `scripts/eplan_converter.py` met werkende write_output() en succesvolle end-to-end run op Stuklijst.xlsx.
</objective>

<execution_context>
@C:/Users/rik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-core-converter/11-CONTEXT.md
@.planning/phases/11-core-converter/11-01-SUMMARY.md

# Referentie voor exacte CSV-kolomstructuren:
@scripts/bom_converter.py
@scripts/app_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stuklijst-duplicaatcheck en write_output()</name>
  <files>scripts/eplan_converter.py</files>
  <action>
Voeg toe aan `scripts/eplan_converter.py`:

**Stuklijst-duplicaatcheck (BOM-01):**

Voeg toe aan `convert()` vóór `write_output()` (en vóór het sluiten van de connectie — heropen indien nodig, of verplaats de close naar na de check):

```python
# Stap 5: Check of stuklijst al bestaat
try:
    conn2, _ = get_connection(env)
    if conn2:
        cursor = conn2.cursor()
        cursor.execute(
            "SELECT COUNT(*) FROM R_ASSEMBLY WHERE CODE = ?",
            (projectnaam,)
        )
        count = cursor.fetchone()[0]
        conn2.close()
        if count > 0:
            result.errors.append(
                f"Stuklijst '{projectnaam}' bestaat al in R_ASSEMBLY. "
                f"Verwijder de bestaande stuklijst in RidderIQ of gebruik "
                f"een andere projectnaam."
            )
except Exception as e:
    result.warnings.append(f"Kan stuklijst-duplicaatcheck niet uitvoeren: {e}")
```

**`write_output(projectnaam: str, rijen: List[EplanRow], result: ConversionResult, env: str)`:**

Output directory: `get_output_basis() / env` (zelfde patroon als bom_converter.py).
Maak de directory aan als die niet bestaat: `output_dir.mkdir(parents=True, exist_ok=True)`.

Hulpfunctie (hergebruik patroon uit bom_converter.py):
```python
def _write_csv(path: Path, headers: list, rows: list):
    with open(path, 'w', newline='', encoding='utf-8-sig') as f:
        writer = csv.writer(f, delimiter=';')
        writer.writerow(headers)
        writer.writerows(rows)
    logging.getLogger(__name__).info(f"  {path.name}: {len(rows)} regels")
```

**Bestand 01 — `01-nieuwe-artikelen-eplan.csv`** (alleen als `result.new_items` niet leeg):

Exacte kolomheaders overnemen van `generate_artikelen()` in `bom_converter.py`:
```
['Artikelcode', 'Omschrijving', 'Omschrijving Engels', 'Artikelgroep', 'Artikeleenheid', 'Registratietraject', 'Stocktype']
```

Per nieuw artikel een rij:
```python
[
    item['new_code'],
    item['omschrijving'],
    item['omschrijving'],          # Engels = zelfde als NL
    EPLAN_ARTIKELGROEP_PK,         # 673
    EPLAN_ARTIKELEENHEID,          # 5 (STUKS)
    EPLAN_REGISTRATIONPATH,        # 5
    EPLAN_INVENTORYKIND,           # 4
]
```

**Bestand 02 — `02-stuklijst-header.csv`** (altijd):

Exacte kolomheaders van `generate_stuklijst_headers()` in `bom_converter.py`:
```
['Stuklijstnummer', 'Omschrijving', 'Tekeningnummer', 'Datum revisie', 'Stuklijst status', 'Revisie', 'Maakdeelvoorkeur', 'Artikelcode']
```

Eén rij (de ePlan stuklijst heeft altijd 1 header):
```python
datum = datetime.now().strftime('%d/%m/%y 00:00:00')
[
    projectnaam,    # Stuklijstnummer = projectnaam uit cel A4
    projectnaam,    # Omschrijving = zelfde
    projectnaam,    # Tekeningnummer = zelfde
    datum,
    EPLAN_STUKLIJST_STATUS,   # 4
    0,              # Revisie = 0 (ePlan kent geen revisies)
    1,              # Maakdeelvoorkeur
    projectnaam,    # Artikelcode = zelfde als stuklijstnummer
]
```

**Bestand 03 — `03-stuklijstregels.csv`** (altijd):

Exacte kolomheaders van `generate_stuklijstregels()` in `bom_converter.py`:
```
['Stuklijstnummer', 'Artikelcode', 'Omschrijving', 'Aantal', 'Posnr', 'Lengte', 'Verstek links', 'Verstek rechts', 'Rotatie']
```

Per component (zowel matched als new_items), gesorteerd op bestelnummer, met Posnr in stappen van 10:
```python
posnr = 10
for comp in alle_componenten:  # matched + new_items, gesorteerd op bestelnummer
    ridderiq_code = comp.get('ridderiq_code') or comp.get('new_code')
    rows.append([
        projectnaam,
        ridderiq_code,
        comp['omschrijving'],
        int(comp['qty']),
        posnr,
        '',     # Lengte — leeg voor elektrische componenten
        '',     # Verstek links
        '',     # Verstek rechts
        '',     # Rotatie
    ])
    posnr += 10
```

Log na elk geschreven bestand via `logging.getLogger(__name__).info()`.
  </action>
  <verify>
    <automated>cd "c:/Users/rik/ritco.be/Ritco BV/Spinnekop" && python -c "
import csv, pathlib
from scripts.eplan_converter import _write_csv, ConversionResult
# Test _write_csv helper
import tempfile, os
tmp = pathlib.Path(tempfile.mktemp(suffix='.csv'))
_write_csv(tmp, ['Artikelcode', 'Omschrijving'], [['26001', 'Test'], ['26002', 'Test2']])
with open(tmp, encoding='utf-8-sig') as f:
    content = f.read()
assert ';' in content, 'Geen semicolon separator'
assert 'Artikelcode' in content
tmp.unlink()
print('OK: _write_csv schrijft correcte CSV (UTF-8-BOM, semicolon)')
# Test kolomheaders identiek aan bom_converter.py
from scripts.eplan_converter import ARTIKEL_HEADERS, HEADER_HEADERS, REGELS_HEADERS
assert ARTIKEL_HEADERS == ['Artikelcode', 'Omschrijving', 'Omschrijving Engels', 'Artikelgroep', 'Artikeleenheid', 'Registratietraject', 'Stocktype'], f'Artikel headers mismatch: {ARTIKEL_HEADERS}'
assert 'Stuklijstnummer' in HEADER_HEADERS
assert 'Stuklijstnummer' in REGELS_HEADERS
print('OK: Kolomheaders identiek aan bom_converter.py structuur')
"
</automated>
    <manual>Verifieer dat de header-constanten letterlijk overeenkomen met de headers in bom_converter.py generate_artikelen(), generate_stuklijst_headers() en generate_stuklijstregels().</manual>
  </verify>
  <done>write_output() is geimplementeerd. Drie CSV-schrijvers gebruiken exacte headers uit bom_converter.py. _write_csv schrijft UTF-8-BOM met semicolon. Stuklijst-duplicaatcheck is ingebouwd in convert().</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integratietest op Stuklijst.xlsx</name>
  <files>scripts/eplan_converter.py</files>
  <action>
Voeg een `__main__` blok toe onderaan `eplan_converter.py` voor handmatig testen:

```python
if __name__ == '__main__':
    import sys
    logging.basicConfig(level=logging.INFO, format='%(message)s')
    log = logging.getLogger(__name__)

    excel = sys.argv[1] if len(sys.argv) > 1 else '20-analyse/Inputdocumenten/Stuklijst.xlsx'
    env = sys.argv[2] if len(sys.argv) > 2 else 'speel'

    log.info(f"ePlan Converter — dry run")
    log.info(f"  Excel: {excel}")
    log.info(f"  Omgeving: {env}")

    result = convert(excel, env, dry_run=True)

    log.info(f"")
    log.info(f"Resultaat:")
    log.info(f"  Matched:    {len(result.matched)}")
    log.info(f"  Nieuw:      {len(result.new_items)}")
    log.info(f"  Overgeslagen: {len(result.skipped)}")
    log.info(f"  Warnings:   {len(result.warnings)}")
    log.info(f"  Fouten:     {len(result.errors)}")

    for w in result.warnings:
        log.warning(f"  WAARSCHUWING: {w}")
    for e in result.errors:
        log.error(f"  FOUT: {e}")

    if result.has_blockers:
        log.error("GEBLOKKEERD — geen bestanden worden geschreven")
        sys.exit(1)
    else:
        log.info("OK — dry run geslaagd")
```

Voer de end-to-end integratietest uit. Er zijn twee mogelijke uitkomsten:

**Scenario A — Geen VPN (verwacht):** De converter geeft een blokkerende fout over de SQL-verbinding. Verifieer dat:
- Geen Python exception/traceback zichtbaar is
- De foutmelding "VPN" of "verbinding" bevat
- `result.has_blockers == True`
- De Excel WEL is ingelezen (projectnaam en rijen zijn gevuld vóór de verbindingsfout)

**Scenario B — Met VPN:** De converter matcht componenten. Verifieer dat:
- `len(result.matched) + len(result.new_items) > 0`
- Geen onverwachte Python exceptions
- Bij nieuwe artikelen: codes beginnen met '26'

Voeg ook een validatie toe in `convert()` voor de Excel-structuur: als `parse_eplan_excel()` slaagt maar geen rijen geeft, voeg dan een niet-blokkerende waarschuwing toe: `"Excel bevat geen dataregels (rij 7+)"`.

Zorg dat de module importeerbaar is zonder side effects (de `if __name__ == '__main__'` guard is verplicht).

**Laatste controle kolomheaders:** Open `bom_converter.py` en vergelijk letterlijk:
- `generate_artikelen()` headers → `ARTIKEL_HEADERS` constante in eplan_converter.py
- `generate_stuklijst_headers()` headers → `HEADER_HEADERS` constante
- `generate_stuklijstregels()` headers → `REGELS_HEADERS` constante

Als er constanten ontbreken in Task 1, voeg ze alsnog toe als module-level constanten.
  </action>
  <verify>
    <automated>cd "c:/Users/rik/ritco.be/Ritco BV/Spinnekop" && python scripts/eplan_converter.py 20-analyse/Inputdocumenten/Stuklijst.xlsx speel</automated>
    <manual>
Controleer output:
1. Geen Python traceback zichtbaar
2. "Resultaat:" sectie toont aantallen (ook al zijn alle 0 bij geen VPN)
3. Bij geen VPN: melding bevat "VPN" of "verbinding" als FOUT
4. Script eindigt met exit code 0 (dry run OK) of 1 (geblokkeerd door verbindingsfout)

Test importeerbaarheid:
python -c "import scripts.eplan_converter; print('Import OK')"
    </manual>
  </verify>
  <done>
- eplan_converter.py draait als script zonder traceback
- Module importeerbaar zonder side effects
- Dry run op Stuklijst.xlsx geeft gestructureerde output (matched/nieuw/warnings/fouten)
- Bij geen VPN: blokkerende fout met duidelijke tekst, geen crash
- Bij VPN: matched + new_items gevuld, codes voor nieuwe artikelen beginnen met '26'
  </done>
</task>

</tasks>

<verification>
python -c "
from scripts.eplan_converter import (
    ConversionResult, EplanRow, parse_eplan_excel, aggregate_rows,
    convert, write_output, _write_csv,
    ARTIKEL_HEADERS, HEADER_HEADERS, REGELS_HEADERS,
    EPLAN_ARTIKELGROEP_PK, EPLAN_REGISTRATIONPATH
)
assert EPLAN_ARTIKELGROEP_PK == 673
assert EPLAN_REGISTRATIONPATH == 5
assert ARTIKEL_HEADERS[0] == 'Artikelcode'
assert HEADER_HEADERS[0] == 'Stuklijstnummer'
assert REGELS_HEADERS[0] == 'Stuklijstnummer'
print('Alle exports en constanten correct')
"
</verification>

<success_criteria>
- write_output() bestaat en produceert correcte CSV-bestanden bij dry_run=False
- Stuklijst-duplicaatcheck blokkeert bij bestaande CODE in R_ASSEMBLY
- 01-nieuwe-artikelen-eplan.csv wordt alleen aangemaakt als new_items niet leeg is
- Alle 3 CSV-bestanden gebruiken semicolon en UTF-8-BOM encoding
- Kolomheaders in alle 3 bestanden zijn letterlijk identiek aan bom_converter.py
- convert(dry_run=True) schrijft nooit bestanden
- Module draait als script zonder traceback op Stuklijst.xlsx
- Alle 12 requirement IDs (PARSE-01..03, MATCH-01..04, ART-01..02, BOM-01..03) zijn gedekt
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/11-core-converter/11-02-SUMMARY.md` aan.
</output>
